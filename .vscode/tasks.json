{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build & Run",
      "type": "shell",
      "command": "cmake -S . -B build && cmake --build build && (cd build && ./main)",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}